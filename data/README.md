# Data Directory

This directory stores datasets used by Spark Compiler:

- `uploads/`: Temporary and user-uploaded datasets (CSV, JSON, Parquet, Delta).
- `samples/`: Bundled sample datasets for testing transformations and compiler jobs.
